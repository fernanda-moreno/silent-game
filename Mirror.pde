class Mirror {

  float x, y, width, height;
  boolean held;

  Mirror(float x, float y, float width, float height) {
    this.x = x;
    this.y = y;
    this.width = width;
    this.height = height;
    held = false;
  }
}
