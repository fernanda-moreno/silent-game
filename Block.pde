class Block {

	float x, y, width, height;
	boolean held;

	Block(float x, float y, float width, float height) {
		this.x = x;
		this.y = y;
		this.width = width;
		this.height = height;
		held = true;
	}

}